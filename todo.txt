check doc page arrangement on page load without hash vs with hash

collection pagination
stash away components and pages from dom
Use FormData on form submission
Check CORS
Subpage targeting [this-goto="page&sub"] without damaging the url


Write documentation
    - Make header fixed
Write tests


before
    page.load (DomElement):Boolean
    collection.render (Object, DomElement):Object|Boolean
    collection.model.render (Object, DomElement):Object|Boolean
    model.delete (Model, DomElement):Boolean
    model.create (Model):Boolean
    model.update (Model):Boolean
    model.render (Object, DomElement):Object|Boolean

Events (element => event name)
    page => page.load.canceled
    collection => collection.render.canceled
    collection => collection.model.render.canceled
    form => model.create.canceled
    form => model.update.canceled
    model => model.delete.canceled
    model => model.render.canceled
