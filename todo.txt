collection pagination
stash away templates from dom, probably in the localStorage
Use FormData on form submission
Check CORS


Further minimize JS
Write documentation
    - Make header fixed
Write tests
use indexdb for caching


before
    page.load (DomElement):Boolean
    collection.render (Object, DomElement):Object|Boolean
    collection.model.render (Object, DomElement):Object|Boolean
    create.form.submission (Object, FormElement):Boolean
    model.delete (DomElement):Boolean
    model.render (Object, DomElement):Object|Boolean
    update.form.submission (Object, FormElement):Boolean
